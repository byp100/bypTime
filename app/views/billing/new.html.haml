- provide(:title, "Dues | Sign-up")
.container
  .jumbotron
    %h2 Dues
    = form_tag({controller: 'billing', action: 'create', method: 'post'})  do
      %article
        - if flash[:error].present?
          #error_explanation
            %p= flash[:error]
      %script.stripe-button{"data-amount" => "1000", "data-description" => "BYP100 monthly dues", "data-key" => Rails.configuration.stripe[:publishable_key], "data-locale" => "auto", :src => "https://checkout.stripe.com/checkout.js"}

